import { Env } from "../env"
import { Pattern } from "../pattern"
import { Value } from "../value"

export function matchPattern(env: Env, pattern: Pattern, value: Value): Env | undefined {
  return undefined
}
