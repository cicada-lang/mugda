import { Exp } from "../exp"
import { Pattern } from "../pattern"
import { Order } from "../termination"

export function compareWithPatten(exp: Exp, pattern: Pattern): Order {
  throw new Error("TODO")
}
